# IoTwizz Dependencies
# ====================

# Core Framework
rich>=13.0.0
prompt-toolkit>=3.0.0

# Serial/Hardware Communication
pyserial>=3.5

# Network Protocols
paho-mqtt>=1.6.0
scapy>=2.5.0
paramiko>=3.0.0
requests>=2.28.0

# AI Providers (install as needed)
google-generativeai>=0.6.0    # For Gemini
openai>=1.0.0                 # For OpenAI, DeepSeek, Ollama, custom endpoints
anthropic>=0.18.0             # For Claude

# Wireless
bleak>=0.21.0                 # For BLE scanning

# Optional System Tools (install via package manager)
# binwalk - Firmware analysis: apt install binwalk / brew install binwalk
# openocd - JTAG/SWD: apt install openocd / brew install openocd
# flashrom - SPI flash: apt install flashrom / brew install flashrom
